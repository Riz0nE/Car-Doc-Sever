/* 
 *install jsonwebtoken
 *jwt.sign(paylode,secret,{expiresIn:})
 * token client

*/

/*
* 1.set cookies with http only,for development secure: false,

* 2.cors 

app.use(cors({
    origin: ['http://localhost:5173'],
    credentials:true
})); 

* 3.client side axios setting
*/ 